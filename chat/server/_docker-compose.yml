version: '3'

services:
  synapse:
    container_name: hermes_chat_synapse
    build: .
    restart: unless-stopped
    environment:
      - SYNAPSE_CONFIG_PATH=/data/homeserver.yaml
      - PYTHONPATH="$${PYTHONPATH}:/var/task"
    volumes:
      - ./files:/data:default
    depends_on:
      - hermes_model_mariadb
    # traefik ignores ports,
    # keep it for k8s/helm charts only. 
    ports:
      - ${HERMES_PORT_PREFIX}03:8448/tcp
    labels:
      - traefik.enable=true
      - traefik.http.routers.http-synapse.entryPoints=http
      - traefik.http.routers.http-synapse.rule=Host(`${HERMES_HOSTNAME}`)
      - traefik.http.middlewares.https_redirect.redirectscheme.scheme=https
      - traefik.http.middlewares.https_redirect.redirectscheme.permanent=true
      - traefik.http.routers.http-synapse.middlewares=https_redirect
      - traefik.http.routers.https-synapse.entryPoints=https
      - traefik.http.routers.https-synapse.rule=Host(`${HERMES_HOSTNAME}`)
      - traefik.http.routers.https-synapse.service=synapse
      - traefik.http.routers.https-synapse.tls=true
      - traefik.http.services.synapse.loadbalancer.server.port=8008
      - traefik.http.routers.https-synapse.tls.certResolver=le-ssl
    networks:
      hermes_net_chat:
        ipv4_address: 10.10.10.4
      hermes_net_model_chat: 

  # move to web clients
  element:
    container_name: hermes_chat_element
    image: vectorim/element-web:latest
    restart: unless-stopped
    # # traefik ignores ports,
    # # keep it for k8s/helm charts only. 
    # ports:
    #   - ${HERMES_PORT_PREFIX}01:80/tcp
    #   - ${HERMES_PORT_PREFIX}02:443/tcp
    volumes:
      - ./element-config.json:/app/config.json
    networks:
      hermes_net_chat:
        ipv4_address: 10.10.10.3

  # bridge-whatsapp:
  #   container_name: hermes_chat_whatsapp
  #   image: dock.mau.dev/mautrix/whatsapp:latest
  #   restart: unless-stopped
  #   volumes:
  #     - ./data/bridge/whatsapp:/data

  # bridge-telegram:
  #   container_name: hermes_chat_telegram
  #   image: dock.mau.dev/mautrix/telegram:latest
  #   restart: unless-stopped
  #   volumes:
  #     - ./data/bridge/telegram:/data

  # bridge-signal:
  #   container_name: hermes_chat_signal
  #   image: dock.mau.dev/mautrix/signal:latest
  #   restart: unless-stopped
  #   volumes:
  #     - ./data/bridge/signal:/data

  # bridge-slack:
  #   container_name: hermes_chat_slack
  #   image: dock.mau.dev/mautrix/slack:latest
  #   restart: unless-stopped
  #   volumes:
  #     - ./data/bridge/slack:/data

  # bridge-meta-messenger:
  #   container_name: hermes_chat_meta-messenger
  #   image: dock.mau.dev/mautrix/meta:latest
  #   restart: unless-stopped
  #   volumes:
  #     - ./data/bridge/meta/messenger:/data

  # bridge-meta-instagram:
  #   container_name: hermes_chat_meta-instagram
  #   image: dock.mau.dev/mautrix/meta:latest
  #   restart: unless-stopped
  #   volumes:
  #     - ./data/bridge/meta/instagram:/data

  # bridge-discord:
  #   container_name: hermes_chat_discord
  #   image: dock.mau.dev/mautrix/discord:latest
  #   restart: unless-stopped
  #   volumes:
  #     - ./data/bridge/discord:/data

networks:
  hermes_net_weagle:
    name: hermes_net_weagle
    driver: bridge
    external: true
  hermes_net_chat:
    name: hermes_net_chat
    driver: bridge
    external: true
  hermes_net_model_chat:
    name: hermes_net_model_chat
    driver: bridge
    external: true